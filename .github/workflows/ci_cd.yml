name: ci
on: push
  jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
    
    -name: Checkout repo
     uses: actions/checkout@v3
    
    -name: Install dependencies
     uses: npm install
    
    -name: Run test
     uses: npm test

  
